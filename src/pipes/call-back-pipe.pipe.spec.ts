import { CallBackPipePipe } from './call-back-pipe.pipe';

describe('CallBackPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CallBackPipePipe();
    expect(pipe).toBeTruthy();
  });
});
